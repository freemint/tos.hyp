!begin_node Version 4.91

!begin_itemize !compressed
!item Beschreibung der (!nolink [XBIOS])-Funktionen: (!nl)
      CJar
!item ct60-XBIOS-Erweiterung, Dank an Didier Mequignon
!item Beschreibung der (!nolink [GEMDOS])-Funktionen: (!nl)
      STEFcntrl
!item Beschreibung der (!nolink [VDI])-Funktionen: (!nl)
      vsf_xperimeter,
      v_etext, vst_ex_load_fonts, vs_bkcolor, v_setrgbi,
      vs_grayoverride, v_pat_rotate,
      v_topbot, v_ps_halftone, v_get_driver_info, vex_wheelv,
      vst_charmap und vst_map_mode getrennt.
!item Bessere Beschreibung der (!nolink [GEM])/3 Erweiterung von v_justified.
!item Nach der (!nolink [GEM])-Doku heiût v_trays v_tray,
      (!nolink [NVDI]) benutzt aber den ersten Namen.
!item Beschreibung der (!nolink [AES])-Funktionen: (!nl)
      pdlg_save_default_settings, fsel_boxinput,
      edit_get_pos, edit_set_pos
!item Beschreibung der (!nolink [Editobjekt-Funktionen]) vervollstÑndigt.
!item Beschreibung der MAGX.INF Datei von (!nolink [MagiC]) aktuallisiert bis Version
      6.20
!item UnterstÅtzung des LTL-Protokoll
!item UnterstÅtzung des SE-Protokoll
!item UnterstÅtzung des System Services Protocol
!item UnterstÅtzung (wieder) des Font-Protokoll, und erweitert auf Revision 5.
!item UnterstÅtzung der xFSL-Schnittstelle
!item Hinweis bei Sversion das (!nolink [Magic]) ab Version 6.20 auch 0.20
      als Gemdos-Version liefert. [Harald Placke]
!item Bitbelegung von scrp_read korrigiert
!item Durch den Vergleich mit der letzten gemlib einige Fehler und Unschînheiten behoben.
!item Bugfix bei Setexc stimmte der Link zum Binding nicht.
!item Bugfix im Binding von v_resize_bm
!item Bugfix bei v_setrgb
!item Bugfix Ssystem: Binding falsch.
!item Viele, viele, sogar sehr viele Links gelîscht und neu gesetzt.
!end_itemize

!end_node

!iflang [english]

!begin_node MRETS

!begin_verbatim
typedef struct
{
    int16_t x;
    int16_t y;
    int16_t buttons;
    int16_t kstate;
} MRETS;
!end_verbatim

See also:
cpx_button ~  cpx_m1  ~ cpx_m2 ~  XCONTROL
!end_node

!else

!begin_node MRETS

!begin_verbatim
typedef struct
{
    int16_t x;
    int16_t y;
    int16_t buttons;
    int16_t kstate;
} MRETS;
!end_verbatim

Querverweis:
cpx_button ~  cpx_m1  ~ cpx_m2 ~  XCONTROL
!end_node

!endif

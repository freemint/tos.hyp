!iflang [english]

!begin_node Salert
(!begin_liste) [Availability]
!item [Name:]
ØSalertÆ - Warnung bzw. Fehlermeldung ausgeben.
!item [Opcode:]
316
!item [Syntax:]
void Salert ( int8_t *msg );

!item [Description:]
Die Funktion gibt eine Fehler- bzw. Warnmeldung aus, die in die
Alert-Pipeline U:\PIPE\ALERT geschrieben wird.

Die Meldung (!I)msg(!i) sollte keine Steuerzeichen, Linefeeds etc.
enthalten. Sie sollte eine einfache einzeilige Warn- oder Fehlermeldung
sein.

Die Funktion formatiert die Meldung selbstÑndig und sendet sie dem Benutzer.
Die genaue Form des Outputs hÑngt jedoch von der verwendeten
Systemkonfiguration ab.

!item [(!nolink [Return]) Value:]
Die Funktion hat kein direktes Ergebnis.

!item [Availability:]
Available when a 'MiNT' cookie with a version of at least 0.98 exists.

!item [Group:]
System Functions

!item [See Also:] (!link [Binding] [Bindings for Salert]) ~ Cconws ~
        Test auf Pipes
(!ende_liste)


!begin_node Bindings for Salert
!ignore_index
(!begin_liste) [Assembler:]
!item [C:]
void Salert ( int8_t *msg );
!item [Assembler:]
!begin_verbatim
pea       msg          ; Offset 2
move.w    #316,-(sp)   ; Offset 0
trap      #1           ; GEMDOS
addq.l    #6,sp        ; correct stack
!end_verbatim
!ifdest [html]
!item [GFA-Basic]
!begin_raw
<A HREF="http://www.milanstation.de/at/gfahutil/gfau7gd.htm">GFA-H-Util GEMDOS</A>
!end_raw
!endif
(!ende_liste)
!end_node
!end_node

!else

!begin_node Salert
(!begin_liste) [Beschreibung]
!item [Name:]
ØSalertÆ - Warnung bzw. Fehlermeldung ausgeben.
!item [Gemdosnummer:]
316
!item [Deklaration:]
void Salert ( int8_t *msg );

!item [Beschreibung:]
Die Funktion gibt eine Fehler- bzw. Warnmeldung aus, die in die
Alert-Pipeline U:\PIPE\ALERT geschrieben wird.

Die Meldung (!I)msg(!i) sollte keine Steuerzeichen, Linefeeds etc.
enthalten. Sie sollte eine einfache einzeilige Warn- oder Fehlermeldung
sein.

Die Funktion formatiert die Meldung selbstÑndig und sendet sie dem Benutzer.
Die genaue Form des Outputs hÑngt jedoch von der verwendeten
Systemkonfiguration ab.

!item [Ergebnis:]
Die Funktion hat kein direktes Ergebnis.

!item [VerfÅgbar:]
Available when a 'MiNT' cookie with a version of at least 0.98 exists.

!item [Gruppe:]
Systemfunktionen

!item [Querverweis:] (!link [Binding] [Bindings fÅr Salert]) ~ Cconws ~
				Test auf Pipes
(!ende_liste)


!begin_node Bindings fÅr Salert
!ignore_index
(!begin_liste) [Assembler:]
!item [C:]
void Salert ( int8_t *msg );
!item [Assembler:]
!begin_verbatim
pea       msg          ; Offset 2
move.w    #316,-(sp)   ; Offset 0
trap      #1           ; GEMDOS aufrufen
addq.l    #6,sp        ; Stack korrigieren
!end_verbatim
!ifdest [html]
!item [GFA-Basic]
!begin_raw
<A HREF="http://www.milanstation.de/at/gfahutil/gfau7gd.htm">GFA-H-Util GEMDOS</A>
!end_raw
!endif
(!ende_liste)
!end_node
!end_node

!endif

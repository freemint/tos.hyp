!iflang [english]

!begin_node Fsetdta
(!begin_liste) [Availability]
!item [Name:]
Øset disk transfer addressÆ - Setzen der Adresse fÅr den öbertragungsbereich
fÅr Diskettenoperationen.
!item [Opcode:]
26
!item [Syntax:]
void Fsetdta ( DTA *buf );

!item [Description:]
Die GEMDOS-Routine Fsetdta setzt die Adresse der aktuellen DTA. Der
Parameter (!I)buf(!i) ist ein Zeiger auf diese Struktur.

(!B)Hinweis:(!b) Bei Programmstart wird die (!nolink [DTA]) ab dem Offset 0x80 in der
Basepage eingerichtet.

!item [(!nolink [Return]) Value:]
Die Funktion liefert kein Ergebnis.


!item [Availability:] All GEMDOS versions.

!item [Group:]
File Functions

!item [See Also:] (!link [Binding] [Bindings for Fsetdta]) ~ Fgetdta ~ 
        Fsfirst ~  BASEPAGE ~ DTA
(!ende_liste)


!begin_node Bindings for Fsetdta
!ignore_index
(!begin_liste) [Assembler:]
!item [C:]
void Fsetdta ( DTA *buf );
!item [Assembler:]
!begin_verbatim
pea       buf          ; Offset 2
move.w    #26,-(sp)    ; Offset 0
trap      #1           ; GEMDOS
addq.l    #6,sp        ; correct stack
!end_verbatim

!item [GFA-Basic]
Fehler%=Gemdos(&H1A,L:buf%)

(!ende_liste)
!end_node
!end_node

!else

!begin_node Fsetdta
(!begin_liste) [Beschreibung]
!item [Name:]
Øset disk transfer addressÆ - Setzen der Adresse fÅr den öbertragungsbereich
fÅr Diskettenoperationen.
!item [Gemdosnummer:]
26
!item [Deklaration:]
void Fsetdta ( DTA *buf );

!item [Beschreibung:]
Die GEMDOS-Routine Fsetdta setzt die Adresse der aktuellen DTA. Der
Parameter (!I)buf(!i) ist ein Zeiger auf diese Struktur.

(!B)Hinweis:(!b) Bei Programmstart wird die (!nolink [DTA]) ab dem Offset 0x80 in der
Basepage eingerichtet.

!item [Ergebnis:]
Die Funktion liefert kein Ergebnis.


!item [VerfÅgbar:] Alle GEMDOS Versionen.

!item [Gruppe:]
Dateifunktionen

!item [Querverweis:] (!link [Binding] [Bindings fÅr Fsetdta]) ~ Fgetdta ~ 
        Fsfirst ~  BASEPAGE ~ DTA
(!ende_liste)


!begin_node Bindings fÅr Fsetdta
!ignore_index
(!begin_liste) [Assembler:]
!item [C:]
void Fsetdta ( DTA *buf );
!item [Assembler:]
!begin_verbatim
pea       buf          ; Offset 2
move.w    #26,-(sp)    ; Offset 0
trap      #1           ; GEMDOS aufrufen
addq.l    #6,sp        ; Stack korrigieren
!end_verbatim

!item [GFA-Basic]
Fehler%=Gemdos(&H1A,L:buf%)

(!ende_liste)
!end_node
!end_node

!endif

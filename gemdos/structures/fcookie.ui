!iflang [english]

!begin_node fcookie

!begin_verbatim
typedef struct f_cookie
{
    struct filesys *fs; /* Filesystem welches diesen Cookie kennt     */
    uint16_t dev;       /* Device-Info (z.B. Rwabs GerÑtenummer)      */
    uint16_t aux;       /* weitere Daten fÅr das Filesystem           */
    int32_t  index;     /* dient zusammen mit dev zur Identifizierung */
                        /* eines Files */
} fcookie;
!end_verbatim

See Also:
FILEPTR ~  FILESYS ~  (!link [XFS-Konzept in MagiC][Das XFS-Konzept von MagiC])
!end_node

!else

!begin_node fcookie

!begin_verbatim
typedef struct f_cookie
{
    struct filesys *fs; /* Filesystem welches diesen Cookie kennt     */
    uint16_t dev;       /* Device-Info (z.B. Rwabs GerÑtenummer)      */
    uint16_t aux;       /* weitere Daten fÅr das Filesystem           */
    int32_t   index;    /* dient zusammen mit dev zur Identifizierung */
                        /* eines Files */
} fcookie;
!end_verbatim

Querverweis:
FILEPTR ~  FILESYS ~  (!link [XFS-Konzept in MagiC][Das XFS-Konzept von MagiC])
!end_node

!endif

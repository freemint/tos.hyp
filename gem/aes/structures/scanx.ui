!iflang [english]

!begin_node SCANX

!begin_verbatim
typedef struct
{
    int8_t   scancode;
    int8_t   nclicks;
    int16_t  objnr;
} SCANX;
!end_verbatim

(!B)Note:(!b) The structure contains the assignment for the key with the
scancode (!I)scancode(!i), which when pressed performs an (!I)nclicks(!i) times
mouse click on the object with the index (!I)objnr.(!i) The end of the table
is marked by a scancode of NULL.

See also: About the AES ~ MagiC ~ XDO_INF ~
(!link [Scancode table][The scancode])
!end_node

!else

!begin_node SCANX

!begin_verbatim
typedef struct
{
    int8_t   scancode;
    int8_t   nclicks;
    int16_t  objnr;
} SCANX;
!end_verbatim

(!B)Hinweis:(!b) Die Struktur enthÑlt die Zuordnung fÅr die Taste mit dem
Scancode (!I)scancode(!i), bei deren BetÑtigung ein (!I)nclicks(!i)-facher
Mausklick auf das Objekt mit der Nummer (!I)objnr(!i) ausgefÅhrt wird. Das
Ende der Tabelle wird durch einen Scancode von Null markiert.

Querverweis: AES ~ MagiC ~ XDO_INF ~ (!link [Scan-Code Tabelle][Der Scan-Code])
!end_node

!endif

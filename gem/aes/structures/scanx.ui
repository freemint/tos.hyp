!iflang [english]

!begin_node SCANX

!begin_verbatim
typedef struct
{
    int8_t   scancode;
    int8_t   nclicks;
    int16_t  objnr;
} SCANX;
!end_verbatim

(!B)Hinweis:(!b) Die Struktur enth„lt die Zuordnung fr die Taste mit dem
Scancode (!I)scancode(!i), bei deren Bet„tigung ein (!I)nclicks(!i)-facher
Mausklick auf das Objekt mit der Nummer (!I)objnr(!i) ausgefhrt wird. Das
Ende der Tabelle wird durch einen Scancode von Null markiert.

See Also: AES ~ MagiC ~ XDO_INF ~ (!link [Scan-code table][The scan-code])
!end_node

!else

!begin_node SCANX

!begin_verbatim
typedef struct
{
    int8_t   scancode;
    int8_t   nclicks;
    int16_t  objnr;
} SCANX;
!end_verbatim

(!B)Hinweis:(!b) Die Struktur enth„lt die Zuordnung fr die Taste mit dem
Scancode (!I)scancode(!i), bei deren Bet„tigung ein (!I)nclicks(!i)-facher
Mausklick auf das Objekt mit der Nummer (!I)objnr(!i) ausgefhrt wird. Das
Ende der Tabelle wird durch einen Scancode von Null markiert.

Querverweis: AES ~ MagiC ~ XDO_INF ~ (!link [Scan-Code Tabelle][Der Scan-Code])
!end_node

!endif

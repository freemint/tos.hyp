!iflang [english]

!begin_node MN_SET

!begin_verbatim
typedef struct
{
    int32_t display;    /* Anzeigeverz”gerung              */
    int32_t drag;       /* Auswahlverz”gerung              */
    int32_t delay;      /* Einfachklick Scroll-Verz”gerung */
    int32_t speed;      /* Scroll-Verz”gerung              */
    int16_t height;     /* Scroll-H”he (Anzahl der darzu-  */
                        /* stellenden Eintr„ge)            */
} MN_SET;
!end_verbatim

(!B)Note:(!b) Alle Verz”gerungszeiten werden in Millisekunden gemessen.

See Also: AES ~ menu_settings
!end_node

!else

!begin_node MN_SET

!begin_verbatim
typedef struct
{
    int32_t display;    /* Anzeigeverz”gerung              */
    int32_t drag;       /* Auswahlverz”gerung              */
    int32_t delay;      /* Einfachklick Scroll-Verz”gerung */
    int32_t speed;      /* Scroll-Verz”gerung              */
    int16_t height;     /* Scroll-H”he (Anzahl der darzu-  */
                        /* stellenden Eintr„ge)            */
} MN_SET;
!end_verbatim

(!B)Hinweis:(!b) Alle Verz”gerungszeiten werden in Millisekunden gemessen.

Querverweis: AES ~ menu_settings
!end_node

!endif

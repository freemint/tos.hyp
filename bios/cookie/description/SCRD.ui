!iflang [english]

!begin_node Cookie, SCRD

(!B)Screen To Disk(!b) V1.10

Redirects (!nolink [GEMDOS]) and (!nolink [BIOS]) output for the console additionally to a 
file.

Uses vectors: TRAP #1, TRAP #13.

Archive name: SCR2DISK.LZH.

!end_node

!else

!begin_node Cookie, SCRD

(!B)Screen To Disk(!b) V1.10

Leitet die (!nolink [GEMDOS])- und (!nolink [BIOS])-Ausgaben fÅr die 
Console zusÑtzlich in eine Datei um.

Benutzte Vektoren: TRAP #1, TRAP #13. 

Archivname: SCR2DISK.LZH

!end_node

!endif
 
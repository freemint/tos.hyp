!iflang [english]

!begin_node Cookie, nTAS

Indicates that (!B)no TAS instructions(!b) are to be executed (due to an 
error in `HyperCache'). Is respected by GEMAR.

!end_node

!else

!begin_node Cookie, nTAS

Zeigt an, daû (!B)keine TAS-Instruktionen(!b) ausgefÅhrt werden 
sollen (wg. Fehler in `HyperCache'). Wird von GEMAR beachtet.

!end_node

!endif
 
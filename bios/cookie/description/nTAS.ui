!iflang [english]

!begin_node Cookie, nTAS

Zeigt an, daû keine TAS-Instruktionen ausgefÅhrt werden 
sollen (wg. Fehler in `HyperCache'). Wird von GEMAR beachtet.

!end_node

!else

!begin_node Cookie, nTAS

Zeigt an, daû keine TAS-Instruktionen ausgefÅhrt werden 
sollen (wg. Fehler in `HyperCache'). Wird von GEMAR beachtet.

!end_node

!endif
 
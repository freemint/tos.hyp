!iflang [english]

!begin_node Cookie, JB2G

(!B)(!nolink [BIOS])2Gdos(!b)

This is a program that outputs all text and HP-PCL output over 
Bconout(0) via GDOS.

The cookie points to an internal structure to configure the TSR 
program with a CPX.

Redirected vectors: Xconstat[0], Xconout[0].

Status: Shareware.

!end_node

!else

!begin_node Cookie, JB2G

(!B)(!nolink [BIOS])2Gdos(!b)

Dieses Programm gibt alle Text-, und HP-PCL-Ausgaben Åber 
Bconout(0) per GDOS aus.

Der Cookie zeigt auf eine interne Struktur, um das TSR per CPX zu
konfigurieren.

Verbogene Vektoren: Xconstat[0], Xconout[0]. 

Status: Shareware.

!end_node

!endif
 
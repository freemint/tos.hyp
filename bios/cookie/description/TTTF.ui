!iflang [english]

!begin_node Cookie, TTTF

(!B)TT2TF(!b) - "Thirty-Two to Twenty-Four"

With this the TT is adjusted to a 24-bit address bus, so that TEMPUS, 
for instance, can run on it. The cookie points to a routine with which 
one can switch between a 24- and 32-bit address bus.


!end_node

!else

!begin_node Cookie, TTTF

(!B)TT2TF(!b) - "Thirty-Two to Twenty-Four

Damit wird dem TT ein 24-Bit-Adressbus verpažt, so daž z.B. TEMPUS drauf
l„uft. Der Cookie zeigt auf eine Routine, mit der zwischen 24- und
32-Bit-Adressbus umgeschalten werden kann.

!end_node

!endif
 
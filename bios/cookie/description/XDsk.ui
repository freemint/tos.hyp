!iflang [english]

!begin_node Cookie, XDsk

The cookie identifies the (!B)X-Disk(!b) (reset-proof RAMdisk with all kinds 
of baubles and tomfoolery). The value contains a bit-vector which 
specifies all devices that are controlled by the X-Disk (similar to 
the system variable _drvbits).

!end_node

!else

!begin_node Cookie, XDsk

Der Cookie identifiziert die (!B)X-Disk(!b) (resetfeste Ramdisk mit allen 
m”glichen Spielereien und Firlefanz). Im Value steht ein Bit-Vektor, der 
alle Devices angibt, die von der X-Disk kontrolliert werden (analog zur 
Systemvariable _drvbits).

!end_node

!endif
 
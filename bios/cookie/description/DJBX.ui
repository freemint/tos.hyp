!iflang [english]

!begin_node Cookie, DJBX

The (!B)DeskJet-Init(!b) (!nolink [BIOS]) extension initializes the DJ automatically 
if the printer is switched on after the computer. 

Hooks into the (!nolink [BIOS])-trap.


!end_node

!else

!begin_node Cookie, DJBX

Die (!B)DeskJet-Ini(!b)t (!nolink [BIOS])-Erweiterung meldet den DJ automatisch ab, wenn
er nachtr„glich eingeschaltet wird.

H„ngt sich in den BIOS-Trap

!end_node

!endif
 
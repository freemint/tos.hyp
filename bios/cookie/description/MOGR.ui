!iflang [english]

!begin_node Cookie, MOGR

(!B)GEMRAM (!b) - Installs (!nolink [GEM]) in RAM

Mofifies _sysbase, _exec_os and as a result all vectors of (!nolink [GEM]).
On successful installation it enters this cookie in the cookie jar.
If a new jar has to be created, a reset routine for deinstallation 
is incorporated.

!end_node

!else

!begin_node Cookie, MOGR

(!B)GEMRAM(!b)  - installiert (!nolink [GEM]) im RAM

Ver„ndert _sysbase, _exec_os und in der 
Folge alle Vektoren des (!nolink [GEM]). Tr„gt sich bei erfolgreicher Installation 
in den Cookiejar ein. Muž ein neuer Jar angelegt werden, wird eine 
Resetroutine zur Deinstallation eingebunden.

!end_node

!endif
 
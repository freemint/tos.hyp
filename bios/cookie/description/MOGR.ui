!iflang [english]

!begin_node Cookie, MOGR

GEMRAM (installiert GEM im RAM). Ver„ndert _sysbase, _exec_os und in der 
Folge alle Vektoren des GEM. Tr„gt sich bei erfolgreicher Installation 
in den Cookiejar ein. Muž ein neuer Jar angelegt werden, wird eine 
Resetroutine zur Deinstallation eingebunden.

!end_node

!else

!begin_node Cookie, MOGR

GEMRAM (installiert GEM im RAM). Ver„ndert _sysbase, _exec_os und in der 
Folge alle Vektoren des GEM. Tr„gt sich bei erfolgreicher Installation 
in den Cookiejar ein. Muž ein neuer Jar angelegt werden, wird eine 
Resetroutine zur Deinstallation eingebunden.

!end_node

!endif
 
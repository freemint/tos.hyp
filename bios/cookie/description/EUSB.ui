!iflang [english]

!begin_node Cookie, EUSB

(!B)Ethernet USB API(!b)

The cookie points to a structure over which communication with 
external USB ethernet drivers takes place.

!end_node

!else

!begin_node Cookie, EUSB

(!B)Ethernet USB API(!b)

Der Cookie zeigt auf eine Struktur, Åber die 
die Kommunikation mit externen USB Ethernet Treibern abgewickelt wird.

!end_node

!endif

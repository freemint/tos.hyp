# Source: Archiv DTCOOK12.LZH

!iflang [english]

!begin_node Cookie, TIME

(!B)Led Panel Time(!b)

The "TIME"-cookie contains a pointer to a WORD-sized variable with 
the current time in the Tgettime format.

!end_node

!else

!begin_node Cookie, TIME

(!B)Led Panel Time(!b)

Der "TIME"-Cookie enth„lt einen Zeiger auf ein word-grože Variable
mit der aktuellen Uhrzeit im Tgettime-Format.

!end_node

!endif
 
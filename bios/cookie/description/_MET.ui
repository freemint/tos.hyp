!iflang [english]

!begin_node Cookie, _MET

(!B)(!nolink [MetaDOS])(!b)

The cookie is only present when the operating system extension
MetaDOS (since Version 2.2) is (!nolink [installed]).

!end_node

!else

!begin_node Cookie, _MET

(!B)(!nolink [MetaDOS])(!b)

Der Cookie ist genau dann vorhanden, wenn die Betriebssystemerweiterung
MetaDOS (ab Version 2.2) installiert ist.

!end_node

!endif

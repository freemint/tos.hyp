!iflang [english]

!begin_node Cookie, DEPI

(!B)DeskPic(!b) (ACC and TSR program)

Install an image as a desktop and program background.

Modifies vectors: (!nolink [VDI]) (number 114, recfill, will be evaluated),
(!nolink [BIOS]) 
(for the purpose of reinstalling the VDI-trap, say after the launch 
of TOS/TTP applications).

!end_node

!else

!begin_node Cookie, DEPI

(!B)DeskPic(!b) (ACC und TSR-Programm) 

Bild als Desktop- und Programmhintergrund anmelden.

Vektoren: (!nolink [VDI]) (ausgewertet wird die Nummer 
114, recfill), (!nolink [BIOS]) (zwecks Neuinstallation des VDI-Traps z.B.
nach Start von TOS/TTP-Anwendungen)).

!end_node

!endif
 
!iflang [english]

!begin_node Cookie, MADN

(!B)NEX-output(!b) - AUTO-folder TSR

The cookie contains the address of a function that is responsible 
for the transmission of data, including (!nolink [control]) codes, via the 
serial port or to the console respectively.

NEX-output is contained only in the NEX package and so is subject 
to the same status. 

Source for NEX (Shareware) and queries regarding NEX: Franz Blaha.

!end_node

!else

!begin_node Cookie, MADN

(!B)NEX-Output(!b) - AUTO-Ordner-TSR.

Der Cookie enthÑlt die Adresse einer 
Funktion, die zum öbertragen von Daten inkl. Steuercodes Åber die serielle 
Schnittstelle respektive auf die Console zustÑndig ist.

NEX-Output ist nur im NEX-Paket enthalten und unterliegt damit dem
gleichen Status.

Bezugsquelle fÅr NEX (Shareware) und Anfragen bzgl. NEX: Franz Blaha, 

!end_node

!endif
 
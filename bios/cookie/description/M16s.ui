!iflang [english]

!begin_node Cookie, M16s

Designates the start of the (!B)CyReL M16 STRUCT(!b)ure, a resident data 
block that contains the I/O vectors and hardware addresses of the 
CyReL M16-1280 TrueColor graphics cards (max. 4 per system). If the 
CyReL GIOS (General IO System) is (!nolink [installed]), this XBRA ID is also 
initialized in the (!nolink [BIOS]), X(!nolink [BIOS]) and (!nolink [GEMDOS])
vectors.

!end_node

!else

!begin_node Cookie, M16s

Bezeichnet den Start der (!B)CyReL M16 STRUCT(!b)ure. Ein residenter 
Datenblock, der die I/O-Vektoren und Hardwareadressen der CyReL M16-1280 
True-Color-Graphikkarten enth„lt (max. 4 pro System). Wenn das CyReL GIOS 
(General IO System) installiert ist, wird diese XBRA-Kennung auch im 
(!nolink [BIOS])-, X(!nolink [BIOS])- und (!nolink [GEMDOS])-Vektor eingetragen.

!end_node

!endif
 
!iflang [english]

!begin_node Cookie, PMMU

(!B)Paged Memory Management Unit(!b) reprogrammer

The cookie is created by programs such as (!I)Outside(!i) or (!I)VRAM(!i),
that make 
use of a PMMU present in the system. The value of the cookie is either 
a NULL-pointer, or represents the entry address of a function handler 
that enables some MMU-oriented manipulations. A NULL-pointer simply 
signals the use of the PMMU, which can be a sign for other programs 
(!I)not(!i) to install themselves.

!end_node

!else

!begin_node Cookie, PMMU

(!B)Paged-Memory-Management-Unit(!b)

Der Cookie wird von Programmen wie (!I)Outside(!i) oder (!I)VRAM(!i)
angelegt, die von der im System vorhandenen PMMU gebrauch machen. Der Wert
des Cookies ist entweder ein Null-Zeiger oder stellt die Einsprungadresse
eines Funktionshandlers dar, der einige MMU-bezogene Manipulationen
ermîglicht. Ein Null-Zeiger signalisiert lediglich die Benutzung der PMMU,
was fÅr andere Programme ein Zeichen sein kann, sich (!I)nicht(!i) zu
installieren.

!end_node

!endif

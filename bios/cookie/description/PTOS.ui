!iflang [english]

!begin_node Cookie, PTOS

(!B)ROMRAM(!b)

Copies (!nolink [TOS]) to TT-RAM, and so accelerates execution and permits 
modifications without having to change the ROMs.

Depending on the set options, ROMRAM hooks into the hdv_rw and the 
reset vector. Planned: Bus error and ikbdsys.

!end_node

!else

!begin_node Cookie, PTOS

(!B)ROMRAM(!b)

Kopiert (!nolink [TOS]) in TT-RAM, beschleunigt dadurch die AusfÅhrung und 
erlaubt Modifikationen, ohne die ROMs Ñndern zu mÅssen).

ROMRAM hÑngt sich je nach den eingestellten Optionen in 'hdv_rw' und den
Reset-Vektor ein. Geplant: Bus-Error und 'ikbdsys'.

!end_node

!endif
 
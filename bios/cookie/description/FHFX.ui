!iflang [english]

!begin_node Cookie, FHFX

(!B)F030HFIX.PRG(!b) - (Falcon030 (!nolink [GEMDOS]) Handle Fix)

Corrects an error in the Falcon's (!nolink [GEMDOS]) which leads to all file 
functions not being able to manage negative device handles (-3 to -1)
(which is why GFA-Basic can no longer print). 

Modified vectors: TRAP #1.

!end_node

!else

!begin_node Cookie, FHFX

(!B)F030HFIX.PRG(!b) (Falcon030-GEMDOS-Handle-Fix)

Korrigiert einen Fehler im GEMDOS des Falcon, der dazu fÅhrt, daû
sÑmtliche Filefunktionen nicht mehr mit den negativen Geraetehandles
(-3 bis -1) zurechtkommen (deswegen kann Gfa-Basic auch nicht mehr drucken).

Verbogene Vektoren: TRAP #1.

!end_node

!endif
 
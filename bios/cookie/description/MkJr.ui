!iflang [english]

!begin_node Cookie, MkJr


The cookie identifies a (!B)Make Cookie Jar(!b) AUTO-folder program which 
installs a cookie jar on all machines (apart from STE, Falcon030 or 
TT030, which already have a cookie jar natively).

The four system cookies are created, where the (!link [_CPU][Cookie, _CPU]) cookie distingui-
shes (cleanly) the processor types 68000, 68010, 68020 and 68030, 
though the other cookies are assigned fixed values (corresponding to 
a [Mega] ST[F[M]]).

!end_node

!else

!begin_node Cookie, MkJr

Der Cookie identifiziert mein (!B)MakeCookieJar(!b) Auto-Ordner-Programm, das auf 
allen Nicht-STEs bzw. Nicht-TTs einen Cookie Jar einrichtet. Die vier 
Systemcookies werden angelegt, wobei
(!link [_CPU][Cookie, _CPU]) die Prozessortypen 68000, 68010, 
68020 und 68030 (sauber) unterscheidet, die anderen Cookies jedoch fixe 
Werte zugewiesen bekommen (entsprechend einem [Mega] ST[F[M]]).

!end_node

!endif
 
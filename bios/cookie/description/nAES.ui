!iflang [english]

!begin_node Cookie, nAES

!label nAES, Cookie of 
(!B)(!nolink [N.AES])(!b) - the (!nolink [AES]) for MiNT

This cookie is created by the (!nolink [AES])-replacement (!nolink [N.AES])  and points to 
the structure N_AESINFO.

!end_node

!else

!begin_node Cookie, nAES

!label nAES, Cookie von 
(!B)(!nolink [N.AES])(!b) - Das (!nolink [AES]) fÅr MiNT

Der Cookie wird vom (!nolink [AES])-Ersatz (!nolink [N.AES]) angelegt und zeigt auf die
Struktur N_AESINFO.

!end_node

!endif

!iflang [english]

!begin_node Cookie, VTFX

(!B)VT52FIX.PRG(!b) for Falcon030

Corrects the errors in the VT52 emulator of the Falcon (!nolink [TOS]) (faulty 
Esc-sequences and scrolling). 

Modified vectors: xconout for CON and RAWCON.

!end_node

!else

!begin_node Cookie, VTFX

(!B)VT52FIX.PRG(!b) fÅr Falcon030.

Korrigiert die Fehler im VT52-Emulator des Falcon-TOS (fehlerhafte
Esc-Sequenzen und falsches Scrolling).

Verbogene Vektoren: xconout fÅr CON und RAWCON.

!end_node

!endif
 
!iflang [english]

!begin_node Cookie, BIGS

BigScreen 2, das neue BigScreen fÅr ST, STe und TT (verbiegt Trap 2, 
XBIOS und VBlank).

!end_node

!else

!begin_node Cookie, BIGS

BigScreen 2, das neue BigScreen fÅr ST, STe und TT (verbiegt Trap 2, 
XBIOS und VBlank).

!end_node

!endif
 
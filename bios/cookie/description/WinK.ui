!iflang [english]

!begin_node Cookie, WinK

(!B)WinKeys(!b)

This makes system-wide (!nolink [control]) of windows available (close, move, 
backdrop, un-/all-/iconify, full, scrolling, size) via Control+number-
keypad shortcuts. (!link [TraPatch][Cookie, TraP]) is a prerequisite! 

Modifies vectors: evnt_multi, wind_set (TraPatch!).

Status: Freeware.

!end_node

!else

!begin_node Cookie, WinK

(!B)WinKeys(!b)

Stellt systemweite Tastatursteuerung der Fenster (close, 
move, backdrop, un-/all-/iconify, full, scrolling, size) Åber Control-
Ziffernblock-Shortcuts zur VerfÅgung. (!link [TraPatch][Cookie, TraP]) wird
vorausgesetzt!

Vektoren: evnt_multi, wind_set (TraPatch!)

Status: Freeware.

!end_node

!endif
 
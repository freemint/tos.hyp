!iflang [english]

!begin_node Cookie, CM16

This cookie is placed by (!B)CM16_VIP.PRG(!b) (the CyReL M16 Versatile 
Installation Program) if one or more CyReL M16-1280 TrueColor graphics 
card(s) is/are (!nolink [installed]) in the system. The cookie then points to 
the start of the M16 STRUCTure (see X M16s). If the lowest value bit 
of the pointer is set (so it points to an (!I)odd(!i) address), then the 
M16 STRUCTure is currently being processed or altered by another 
program. The LST (!I)must(!i) be checked before every access and set with a 
READ/MODIFY/WRITE instruction. After processing the M16 STRUCTure 
the MSB is reset again.

!end_node

!else

!begin_node Cookie, CM16

Dieses Cookie wird von dem (!B)CM16_VIP.PRG(!b) (CyReL M16 Versatile Installation 
Program) eingetragen, falls eine oder mehrere CyReL M16-1280 
True-Color-Graphikkarte(n) im System installiert ist/sind. Der Cookie 
zeigt dann auf den Anfang der M16-STRUCTure (siehe X M16s). Ist das 
niederwertigste Bit des Pointers gesetzt (zeigt also auf eine (!I)UNGERADE(!i)
Adresse) dann wird die M16-STRUCTure gerade von einem anderem 
Programm bearbeitet/verÑndert. Das LSB (!I)muss(!i) vor jedem Zugriff geprÅft 
und mit einer READ/MODIFY/WRITE Instruktion gesetzt werden. Nach der 
Bearbeitung der M16-STRUCTure wird das MSB wieder zurÅckgesetzt.

!end_node

!endif
 
!iflang [english]

!begin_node Cookie, HSND
!label Cookie, HSnd

(!B)CrazySounds(!b)

Assigns sound sample outputs to operating system actions, key-presses
etc.

Vectors: (!nolink [GEMDOS; BIOS; AES/VDI]); IKBD/MIDI; mouse (KBDVECS.mousevec); 
bell_hook; kcl_hook; bus errors; address errors; illegal commands; 
ST-MFP I13 (Timer A).

Distribution by MAXON Computer GmbH.

!end_node

!else

!begin_node Cookie, HSND
!label Cookie, HSnd

(!B)CrazySounds(!b)

Belegt Betriebssystemaktionen, TastendrÅcke usw. mit Sampleausgaben.

Vektoren: (!nolink [GEMDOS; BIOS; AES/VDI]); IKBD/MIDI; Maus 
(KBDVECS.mousevec); bell_hook; kcl_hook; Busfehler; Adressfehler; 
illegaler Befehl; ST-MFP-I13 (Timer A).

Vertrieb durch MAXON Computer GmbH.

!end_node

!endif
 
# Dokumentation ST-Magazin 7/91

!iflang [english]

!begin_node Cookie, VSCR

!label Screen, virtual
!label Virtual screen
(!B)Virtual Screen(!b) structure

The cookie contains a pointer to a structure that makes information 
available about the visible portion of the screen.

!end_node

!else

!begin_node Cookie, VSCR

!label Bildschirm, virtueller
!label virtueller Bildschirm
(!B)Virtueller-Bildschirm(!b)

Der Cookie enthÑlt einen Zeiger auf eine Struktur, die Informationen Åber
den (!link [sichtbaren Bildschirmausschnitt][Virtual-Screen-Protokoll])
zur VerfÅgung stellt.

!end_node

!endif

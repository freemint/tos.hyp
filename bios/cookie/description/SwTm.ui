!iflang [english]

!begin_node Cookie, SwTm

(!B)Show Time(!b) - The ultimate informative clock

Runs in all Atari resolutions. Shows the time, free RAM (also with 
MagiC), the date, CapsLock-/DCF status etc.

Hotkeys for clock-switching with Turbo cards or Mega STEs. CapsLock 
LED. With INF file, display and Turbo on/off-switchable, movable.

The cookie points to a data structure with which ShowTime can be 
swiched on/off.

Modifies vectors: (!nolink [GEMDOS])-trap, (!nolink [VDI/AES])-trap, IKBD. 

Copyright status: Freeware/Postcardware.

!end_node

!else

!begin_node Cookie, SwTm

(!B)Show Time(!b)

Die ulti-/informative Uhr.Alle Atari-Auflîsungen. Zeigt Uhrzeit, freies 
RAM (auch bei MagiC), das Datum, CapsLock-/DCF-Status usw.

Hotkeys fÅr Taktumschaltung bei Turbokarten oder Mega STEs. CapsLock LED. Mit 
INF-Datei Anzeige und Turbo an-/abschalt-/(verschieb)bar.

Der Cookie zeigt auf eine Datenstruktur mit der ShowTime an-/ausgeschaltet
werden kann. 

Verbogene Vektoren: (!nolink [GEMDOS])-Trap, (!nolink [VDI/AES])-Trap, IKBD.

Copyrightstatus: Freeware/Postcardware

!end_node

!endif
 
!iflang [english]

!begin_node Cookie, NEXB

(!B)NEX-BUFFER(!b)

This reserves a block of memory of a fixed size and links it via a 
cookie structure.

No vectors are modified.

Status: NEX-BUFFER is a component of 'Maustausch-Externals' NEX 
(Shareware) and is only available with this package, i.e. NEX-BUFFER 
may not be distributed separately.

!end_node

!else

!begin_node Cookie, NEXB

(!B)NEX-BUFFER(!b)

Reserviert einen Speicherblock fixer Grîûe und verkettet 
ihn Åber eine Cookie-Struktur.

Vektoren werden nicht verbogen.

Status: NEX-BUFFER ist Bestandteil des Maustausch-Externals NEX (Shareware)
und ist nur mit diesem Paket verfÅgbar, d.h. NEX-BUFFER darf getrennt nicht
vertrieben werden.

!end_node

!endif
 
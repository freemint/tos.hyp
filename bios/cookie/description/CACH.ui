!iflang [english]

!begin_node Cookie, CACH

(!B)Cache Control(!b)

AUTO-folder TSR. The cookie points to a table with functions for 
managing the CPU caches, starting from the 68020 up to the 68060. 

Cache Control is Freeware.

!end_node

!else

!begin_node Cookie, CACH

(!B)Cache Control(!b)

AUTO-Ordner-TSR. Der Cookie zeigt auf eine Tabelle mit 
Funktionen zur Verwaltung der CPU-Caches, angefangen beim 68020 bis hin
zum 68060.

Cache-Control ist Freeware.

!end_node

!endif
 
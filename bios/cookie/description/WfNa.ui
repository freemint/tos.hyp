!iflang [english]

!begin_node Cookie, WfNa

WFNAME.PRG erweitert MagiC um eine spezielle Funktion. Es erm”glicht
anderen Programmen, den Fenstertitel beliebiger Fenster abfragen zu
k”nnen.

Fr Programmierer:

- Sofern WFNAME.PRG installiert ist, kann man Fenstertitel beliebiger
  Fenster mit wind_get(WF_NAME) erfragen.

- WFNAME.PRG gibt sich durch einen 'WfNa'-Cookie zu erkennen. Abfrage,
  ob wind_get(WF_NAME) m”glich ist, sollte daher so erfolgen:

  'WfNa'-Cookie vorhanden ODER
  'AmAN'-Cookie vorhanden ODER
  MagiC >= 6.0 vorhanden.

!end_node

!else

!begin_node Cookie, WfNa

WFNAME.PRG erweitert MagiC um eine spezielle Funktion. Es erm”glicht
anderen Programmen, den Fenstertitel beliebiger Fenster abfragen zu
k”nnen.

Fr Programmierer:

- Sofern WFNAME.PRG installiert ist, kann man Fenstertitel beliebiger
  Fenster mit wind_get(WF_NAME) erfragen.

- WFNAME.PRG gibt sich durch einen 'WfNa'-Cookie zu erkennen. Abfrage,
  ob wind_get(WF_NAME) m”glich ist, sollte daher so erfolgen:

  'WfNa'-Cookie vorhanden ODER
  'AmAN'-Cookie vorhanden ODER
  MagiC >= 6.0 vorhanden.

!end_node

!endif
 
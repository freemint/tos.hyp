!iflang [english]

!begin_node Cookie, Lace

LaceScan

LaceScan is a package that allows an ordinary ST to display a much larger
(more pixels that is) screen area than that ordinarily used by each of the
three original resolutions, and also to emulate even larger resolutions
than even this enlarged screen can display physically.

!end_node

!else

!begin_node Cookie, Lace

LaceScan

LaceScan is a package that allows an ordinary ST to display a much larger
(more pixels that is) screen area than that ordinarily used by each of the
three original resolutions, and also to emulate even larger resolutions
than even this enlarged screen can display physically.

!end_node

!endif
 
!iflang [english]

!begin_node Cookie, INSJ

(!B)Insjar(!b)

This installs a cookie jar or extends an already existing one. The 
five standard cookies are (!nolink [installed]). (!link [_CPU][Cookie, _CPU])
and (!link [_FPU][Cookie, _FPU]) are filled with 
values corresponding to the (!nolink [installed]) hardware. If a 68030 board is 
present, then _longframe is set accordingly.

!end_node

!else

!begin_node Cookie, INSJ

(!B)Insjar(!b)

Installiert einen Cookie-Jar oder erweitert einen bereits 
bestehenden. Die fÅnf Standard-Cookies werden eingetragen.
(!link [_CPU][Cookie, _CPU]) und (!link [_FPU][Cookie, _FPU]) 
werden mit den Werten entsprechend der installierten Hardware versorgt. 
Falls ein 68030 Board vorhanden ist, wird _longframe entsprechend 
gesetzt.

!end_node

!endif
 
!iflang [english]

!begin_node Cookie, DECD

(!B)DECDriver(!b)

Resident hardware driver for DEC Ethernet cards on the PCI-bus with  
complete PCI-interrupt handling. Runs only on computers with a PCI-bus 
(Hades,...); PCI-(!nolink [BIOS]) (_PCI) is a prerequisite.

Modifies TT-MFP interrupt vectors, depending on how many, and in which 
PCI slots, the network cards have been plugged - this info is offered 
by the PCI-(!nolink [BIOS]).

Copyright: Shareware.

!end_node

!else

!begin_node Cookie, DECD

(!B)DECDriver(!b)

Eesidenter Hardware-Treiber fÅr DEC-Ethernetkarten am PCI-Bus, 
komplette PCI-Interrupt-Verarbeitung, lÑuft nur auf Rechnern mit PCI-Bus 
(Hades,...), PCI-(!nolink [BIOS]) (PCI_) wird vorausgesetzt.

Vektoren: 
TT-MFP-Interrupt-Vektoren, abhÑngig davon, wie viele, und in welchen 
PCI-Slots Netzwerkkarten stecken - diese Infos werden vom PCI-(!nolink [BIOS]) 
bereitgestellt.

Copyright: Shareware

!end_node

!endif
 
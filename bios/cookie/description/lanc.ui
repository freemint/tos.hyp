!iflang [english]

!begin_node Cookie, lanc

(!B)MiNT-Net ethernet driver for AMD LANCE(!b)

lance.xif, pamintrn.xif, rieblmst.xif, rieblmst_fast.xif, 
rieblspc.xif, rieblspc_fast.xif, rieblste.xif, riebltt.xif - 
these are a part of the Ethernet driver of the MiNT-Net package 
for AMD LANCE-based Ethernet cards (PAMsNet, Riebl). 

Affects vectors: int2 (HBI), int4 (VBI), int5.

!end_node

!else

!begin_node Cookie, lanc

(!B)MiNT-Net Ethernet-Treiber fÅr AMD LANCE(!b)

lance.xif, pamintrn.xif, rieblmst.xif, rieblmst_fast.xif, rieblspc.xif, 
rieblspc_fast.xif, rieblste.xif, riebltt.xif - es handelt sich hierbei 
um einen Teil der Ethernet-Treiber des MiNT-Net-Paketes fÅr AMD LANCE 
basierte Ethernet-Karten (PAMsNet, Riebl).

Vektoren: int2 (HBI), int4 (VBI), int5.

!end_node

!endif
 
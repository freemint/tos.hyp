!iflang [english]

!begin_node Cookie, cVDI

This descriptor is used by the (!B)CyReL M16-1280 (!nolink [VDI])(!b) driver. It is 
placed in the (!I)VDI(!i)-trap. The cookie points to a parameter block in 
the (!I)(!nolink [VDI])(!i) driver containing information about the current resolution, 
mode, Line-A configuration and other matters.

!end_node

!else

!begin_node Cookie, cVDI

Dieser Bezeichner wird vom (!B)CyReL M16-1280 (!nolink [VDI])(!b)-Treiber verwendet. Er wird 
im (!B)VDI(!b)-Trap eingetragen. Das Cookie zeigt auf einen Parameterblock im 
(!B)(!nolink [VDI])(!b)-Treiber, der Informationen Åber die momentane Auflîsung, Modus, Line-A 
Konfiguration u.a. enthÑlt.

!end_node

!endif
 
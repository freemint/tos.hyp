!iflang [english]

!begin_node Cookie, LOGB

Logbuch, startet mittels Programmhistory in einem Popup Programme 
nach. H„ngt im GEM-Trap und wartet auf shel_write()-Aufrufe. Freeware

!begin_verbatim
typedef struct
{
   int16_t numln;
   int16_t reserved1;
   int16_t reserved2;
   int16_t reserved3;
   int16_t reserved4;
   int16_t reserved5;
   int16_t reserved6;
   int8_t  **lnptr;
} HISTREC;
!end_verbatim
(!I)Lnptr(!i) zeigt auf ein Feld von maximal (!I)numln(!i) nullterminierten Strings, 
die die Programmnamen mit vollem Pfad enthalten. Sind es weniger als 
numln Eintr„ge, so endet die Liste mit einem Leerstring.

!end_node

!else

!begin_node Cookie, LOGB

Logbuch, startet mittels Programmhistory in einem Popup Programme 
nach. H„ngt im GEM-Trap und wartet auf shel_write()-Aufrufe. Freeware

!begin_verbatim
typedef struct
{
   int16_t numln;
   int16_t reserved1;
   int16_t reserved2;
   int16_t reserved3;
   int16_t reserved4;
   int16_t reserved5;
   int16_t reserved6;
   int8_t  **lnptr;
} HISTREC;
!end_verbatim
(!I)Lnptr(!i) zeigt auf ein Feld von maximal (!I)numln(!i) nullterminierten Strings, 
die die Programmnamen mit vollem Pfad enthalten. Sind es weniger als 
numln Eintr„ge, so endet die Liste mit einem Leerstring.

!end_node

!endif
 
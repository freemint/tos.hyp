!iflang [english]

!begin_node Cookie, PRMD

(!B)MIDIPRT(!b) - Current version 0.9

Redirects the (!nolink [BIOS]) output of the parallel port to the MIDI port.
A receiving program on the destination computer then outputs the 
received data on the printer.

Uses the xbcon... vectors.

!end_node

!else

!begin_node Cookie, PRMD

(!B)MIDIPRT(!b) - Aktuelle Version 0.9

Leitet die (!nolink [BIOS])-Ausgabe der parallelen Schnittstelle auf den 
MIDI-Port um. Ein Empfangsprogramm auf dem Zielrechner gibt die 
empfangenen Daten dann auf den Drucker aus.

Benutzt die xbcon...-Vektoren.

!end_node

!endif
 
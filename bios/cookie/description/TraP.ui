!iflang [english]

!begin_node Cookie, TraP

(!B)TraPatch(!b)

This enables hooking into individual operating system calls, so that 
it is no longer necessary to hook into a trap and through this slow 
down other OS calls. Internal XBRA chains are used for which the 
XBRA IDs of this list are valid!

Modifies vectors: (!nolink [GEMDOS]), (!nolink [GEM]), (X)(!nolink [BIOS]).

Copyright: Freeware

!end_node

!else

!begin_node Cookie, TraP

(!B)TraPatch(!b)

Ermîglicht es sich in einzelne BS-Calls einzuhÑngen, so daû 
es nicht mehr nîtig ist, sich in direkt einen Trap einzuhÑngen und dadurch 
andere BS-Calls abzubremsen. Es werden intern XBRA-Ketten benutzt, fÅr die 
die XBRA-IDs dieser Liste gelten!

VerÑnderte Vektoren: (!nolink [GEMDOS]), (!nolink [GEM]), (X)(!nolink [BIOS]). 


Copyright: Freeware

!end_node

!endif
 
!iflang [english]

!begin_node Cookie, CT60

(!B)CT60 present(!b)
!begin_verbatim
typedef struct
{
  unit16_t trigger_temp;
  unit16_t daystop;
  unit16_t timestop;
  unit16_t speed_fan;
  unsigned long cpu_frequency;        /* in MHz * 10 */
  unit16_t beep;
} CT60_COOKIE;
!end_verbatim

!end_node

!else

!begin_node Cookie, CT60

(!B)CT60 ist vorhanden(!b)
!begin_verbatim
typedef struct
{
  unit16_t trigger_temp;
  unit16_t daystop;
  unit16_t timestop;
  unit16_t speed_fan;
  unsigned long cpu_frequency;        /* in MHz * 10 */
  unit16_t beep;
} CT60_COOKIE;
!end_verbatim

!end_node

!endif

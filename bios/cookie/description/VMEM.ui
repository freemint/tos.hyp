!iflang [english]

!begin_node Cookie, VMEM

(!B)VMEM.PRG(!b) - Provides virtual memory for every Atari computer

Naturally it does not emulate an MMU, but thanks to the function 
interface one can manage a "manually" controled virtual memory, which 
in addition really works on every computer. The cookie value is the 
address of the function handler.

All routines (Pure-C source) are freely available. Archive VMEM11.ZIP.


!end_node

!else

!begin_node Cookie, VMEM

(!B)VMEM.PRG(!b) - realisiert virtuellen Speicher fÅr jeden Atari-Rechner.

NatÅrlich wird dadurch keine MMU emuliert, aber dank der
Funktionsschnittstelle kann man sozusagen einen "manuell" gesteuerten virtuellen
Speicher verwalten, der zudem auf wirklich jedem Rechner funktioniert.
Der Cookie-Wert ist die Adresse des Funktionshandlers.

SÑmtliche Routinen (Pure C-Source) sind frei verfÅgbar. Archiv VMEM11.ZIP

!end_node

!endif
 
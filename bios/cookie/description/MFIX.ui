!iflang [english]

!begin_node Cookie, MFIX

(!B)MONOFIX.PRG(!b) for Falcon 030

If an SM124/44/46 (!nolink [monitor]) is connected, this sets the system variable 
$44c to the correct value 2 (Maccel then saves correctly) and changes 
the palette size in the (!nolink [VDI]) work_out field also to 2. With this some 
older programs will work again which previously believed they could 
not run in ST-high. 

Modified vectors: TRAP #2.

!end_node

!else

!begin_node Cookie, MFIX

(!B)MONOFIX.PRG(!b) fÅr Falcon 030.

Setzt, wenn ein SM124/44/46 angeschlossen ist, 
die Systemvariable $44c auf den korrekten Wert 2 (Maccel schont dann 
richtig) und Ñndert die Palettengrîûe im (!nolink [VDI]) work_out-Feld
ebenfalls auf 2. Damit funktionieren einige Ñltere Programme, die vorher
meinten, sie liefen nicht in ST-hoch, wieder.

Verbogene Vektoren: TRAP #2.

!end_node

!endif
 
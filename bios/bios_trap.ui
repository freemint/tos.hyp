!iflang [english]

!begin_node bios-trap
!label bios
(!begin_liste) [Return Value]
!item [Name:]
ØBiosÆ - Execute bios-trap

!item [Syntax:]
LONG bios ( VOID, ... );

!item [Description:]
The routine bios executes a TRAP #13 exception. The parameters passed depend
on the particular (!nolink [BIOS]) function in each case.


!item [(!nolink [Return]) value:]
The function returns a value of the data type LONG.

!item [See also:] gemdos ~  xbios ~  BIOS ~ (!link [Dispatcher][BIOS Dispatcher])

(!ende_liste)
!end_node

!else

!begin_node bios-Trap
!label bios
(!begin_liste) [Beschreibung]
!item [Name:]
ØbiosÆ - Bios-Trap ausfÅhren.

!item [Deklaration:]
LONG bios ( VOID, ... );

!item [Beschreibung:]
Die Routine bios fÅhrt einen TRAP #13 durch. Die
Åbergebenen Parameter hÑngen von der jeweiligen (!nolink [BIOS])-
Funktion ab.

!item [Ergebnis:]
Die Funktion liefert als Ergebnis einen Wert vom Datentyp LONG.

!item [Querverweis:]    gemdos ~  xbios ~  BIOS ~ (!link [Dispatcher][BIOS-Dispatcher])

(!ende_liste)
!end_node

!endif

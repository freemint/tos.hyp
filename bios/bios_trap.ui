!iflang [english]

!begin_node bios-Trap
!label bios
(!begin_liste) [Return Value]
!item [Name:]
¯bios® - Bios-Trap ausfhren.

!item [Syntax:]
LONG bios ( VOID, ... );

!item [Description:]
Die Routine bios fhrt einen TRAP #13 durch. Die
bergebenen Parameter h„ngen von der jeweiligen (!nolink [BIOS])-
Funktion ab.

!item [(!nolink [Return]) Value:]
Die Funktion liefert als Ergebnis einen Wert vom Datentyp LONG.

!item [See Also:] gemdos ~  xbios ~  BIOS ~ (!link [Dispatcher][BIOS-Dispatcher])

(!ende_liste)
!end_node

!else

!begin_node bios-Trap
!label bios
(!begin_liste) [Beschreibung]
!item [Name:]
¯bios® - Bios-Trap ausfhren.

!item [Deklaration:]
LONG bios ( VOID, ... );

!item [Beschreibung:]
Die Routine bios fhrt einen TRAP #13 durch. Die
bergebenen Parameter h„ngen von der jeweiligen (!nolink [BIOS])-
Funktion ab.

!item [Ergebnis:]
Die Funktion liefert als Ergebnis einen Wert vom Datentyp LONG.

!item [Querverweis:]    gemdos ~  xbios ~  BIOS ~ (!link [Dispatcher][BIOS-Dispatcher])

!endif
